---
layout: post
title: "무신사 코테 1차 회고"
date: 2026-02-19 00:00:00 +0900
---

## Node.js 입력 처리 (Buffering Strategy)

- 브랜드정보 → 고객 요청 순서로 들어오는데 고객 요청을 처리하려면 브랜드 사이즈 표가 먼저 전부 메모리에 있어야 한다.
- 따라서 데이터 간에 의존 관계가 있기 때문에 전체 입력을 버퍼링 한 뒤 파싱하는 방식이 적합하다고 생각했다.

---

## 1번 문제

### 로직 정리
```js
1. 브랜드 개수(B)와 고객 수(Q)를 받는다
2. 각 브랜드의 사이즈 표를 저장한다
3. 고객마다 원하는 브랜드 + 본인 치수를 받아서
   ├─ 브랜드가 없으면 → UNKNOWN
   ├─ 3부위 다 범위 안에 드는 사이즈가 있으면 → 그 중 가장 작은 거
   └─ 맞는 게 없으면:
       ├─ 3부위 다 제일 큰 사이즈보다 크면 → UP
       ├─ 3부위 다 제일 작은 사이즈보다 작으면 → DOWN
       └─ 그 외 → MISMATCH
```

### 사고
- 지금 입력이 쉽표로 이루어진 구조이므로 브랜드명으로 바로 꺼낼 수 있는 구조로 변경하기 위해 HashMap을 사용했다.
1. 만약 배열의 형태로 간다면 배열이 하나씩 순회하면서 찾아하지만 HashMap을 사용한다면 key로 매칭해서 찾으면 되어 O(1)로 작아진다.

- 고객별 판정 방법
1. 고객의 요청이 들어왔지만, 브랜드의 정보가 없다 -> UNKNOWN
2. 사이즈 표를 가장 작은 것부터 확인해서 키 & 가슴 & 허리 순으로 확인해서 맞으면 그 사이즈 출력
3. 사이즈 표를 확인했는데 안맞으면
1) 제일 큰 사이즈의 최대보다 다 초과 -> UP
2) 제일 작은 사이즈 최소보다 다 미만 -> DOWN
3) 나머지는 MISMATCH

### 코드해설
> "각 터미널의 코드 라인 간의 영향을 안준다"

1. B(브랜드 수)와 Q(고객 수)는 solution 함수 호출 전에 inputData[0]에서 파싱되어 파라미터로 전달되고, inputData[0]은 이미 사용했으므로 lineIdx는 1부터 시작한다.
2. 버퍼 형태로 받기 때문에 입력 라인 위치는 lineIdx로 관리한다.
3. 다음 줄로 이동하여 브랜드명과 사이즈 개수를 받고 분리한다.
4. 브랜드명을 brands 해시맵의 key로 저장한다.
5. 다음 줄로 이동하여 첫 번째 요소를 사이즈 라벨, 나머지를 h_min, h_max, c_min, c_max, w_min, w_max로 매핑한 객체를 만들어 sizeList 배열에 push한다.
6. 고객 수만큼 순회하며
   - 브랜드가 해시맵에 없으면 UNKNOWN 출력 후 continue
   - 있으면 해당 sizeChart를 find()로 순회하여 3부위 모두 범위 안에 드는 첫 번째 사이즈를 찾아 출력한다.
7. 못 찾으면 smallest(첫 번째), largest(마지막) 사이즈를 꺼낸다.
8. isUp: 3부위 모두 largest 최대값 초과 → UP, isDown: 3부위 모두 smallest 최소값 미만 → DOWN, 둘 다 아님 → MISMATCH

---
## 2번 문제

### 로직 정리
```js
1. 스토어 수(S)와 예약 요청 수(R)를 받는다
2. 각 스토어의 운영 정보(오픈, 마감, 시간당 한도)를 저장한다
3. 각 스토어의 재고 정보(상품별 수량)를 저장한다
4. 예약 요청을 순서대로 처리하며:
   ├─ 스토어가 없으면 → FAIL,STORE
   ├─ 운영 시간 밖이거나 해당 시간대 한도 초과 → FAIL,TIME
   ├─ 재고 부족 → FAIL,STOCK
   └─ 전부 통과 → OK
       - 재고 차감
       - 해당 시간대 예약 수 +1
       - 성공 카운트 +1
5. 마지막에 총 성공 건수 출력
```

### 사고
- HashMap 사용한다.

- 우선순위 순서대로 검사한다.
  1. 스토어 존재 여부 → 없으면 FAIL,STORE
  2. 운영 시간 범위 & 시간대 한도 → 안 맞으면 FAIL,TIME
  3. 재고 확인 → 부족하면 FAIL,STOCK
  4. 걸리면 바로 continue하여 불필요한 검사를 생략한다.

- 1번 문제와 달리 상태가 변한다.
  1. 실패한 요청은 아무것도 건드리지 않는다.
  2. 모든 검증을 통과했을 때만 재고 차감, 예약 수 증가, 성공 카운트 증가를 수행한다.
  3. 앞선 요청의 결과가 뒤 요청의 판정에 영향을 주므로 요청 순서가 중요하다.

### 코드해설
> "각 터미널의 코드 라인 간의 영향을 준다"

1. data[0]으로 파싱하고 lineIdx는 0부터 시작한다.
2. S번 반복하며 스토어 ID를 key로, 운영 정보 + 빈 stock/reservations 객체를 value로 해시맵에 저장한다.
3. 다시 S번 반복하며 각 스토어의 재고 정보를 콜론(:)으로 split하여 stock 객체에 채운다.
4. R번 반복하며 예약 요청을 처리한다:
   - 요청 데이터를 파싱하고, 시간은 분을 버리고 시(hour)만 추출한다.
   - STORE 검증: 해시맵에 없으면 FAIL,STORE 출력 후 continue
   - TIME 검증: 시간 범위 밖이거나 해당 시간대 예약 수가 한도 이상이면 FAIL,TIME 출력 후 continue
   - STOCK 검증: 재고가 요청 수량보다 적으면 FAIL,STOCK 출력
   - 전부 통과하면 재고 차감, reservations[시간대] +1, successCount +1, OK 출력
5. 반복 종료 후 successCount 출력